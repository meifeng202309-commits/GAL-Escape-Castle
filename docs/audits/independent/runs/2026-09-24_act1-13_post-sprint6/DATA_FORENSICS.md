# DATA_FORENSICS — ACT1–13 post-Sprint6

Baseline: `2acfe324d05c8bea2fb96d7132ba29f894270b38`

## J1. Representative histories

### ACT1 private first action
Reconstructable from:
- player progress locked choice/timestamp;
- formal runtime events / facts;
- observations/knowledge generated by actual consequence.

Result: reconstructable.

### ACT2 discussion / meeting route
Reconstructable from:
- discussion session;
- messages;
- vote decisions/rounds;
- route result;
- runtime event provenance.

Result: reconstructable.

### Library puzzle
Reconstructable from:
- append-only attempts with actor/time/correctness;
- group items;
- hint events;
- current resolution.

Result: reconstructable.

### ACT4/5 route
Reconstructable from:
- locked player choices;
- discussion/votes;
- post-inspection vote ledger;
- route state and formal events.

Result: reconstructable after Sprint3B remediation.

### ACT6/7/8
Behavior/game inputs are largely reconstructable from:
- `s5_votes`;
- `s5_rounds`;
- ACT8 private choices;
- discussion messages;
- selected runtime events.

But not every phase transition has its own append-only event; some exact phase chronology is inferred from created/updated rows rather than directly recorded.

### ACT9 Great Hall
Choices are durable by phase/round/player. Wrong-door penalty events are durable. Discussion messages/sessions are durable.

However current discussion completion can be consumed by generic Sprint2 owner (IDA-001).

### ACT10 branch
Private choices and final group choices are durable; branch booleans/current state are recoverable. Golden Key group item durable.

### ACT11 allocation
All allocation attempts are append-only; current valid allocation separate.

### ACT12 pressure
Station tasks, ENGAGE roles, pressure choices, role-at-event and response latency are durable. System cinematic result is explicitly non-player.

### ACT12 cinematic / formal audio
Current `s6_run_state` preserves only latest cinematic stage/timestamp/audio key. Prior stages/audio triggers are overwritten and browser playback success/failure is not appended to the formal event history.

Result: incomplete chronology — IDA-005.

## J2. Player behavior evidence

The current model can distinguish:
- private first choices;
- discussion messages;
- group votes;
- route choices;
- allocation attempts;
- pressure choice;
- response latency;
- role context.

No evidence was found that system fallback/Teacher resolution is written as a player's behavior choice in the frozen baseline.

## J3. System / fallback / Teacher provenance

Dedicated provenance exists for:
- system fallback;
- group-majority resolution;
- Teacher Override;
- invalidated scopes;
- upstream Teacher context;
- cinematic auto-resolution.

This is materially stronger than the pre-remediation Sprint3B baseline.

## J4. Run / context identity

Most formal evidence carries:
- run_id;
- discussion_session_id where relevant;
- phase/round/step in decision records;
- actor;
- timestamps;
- validity/context where applicable.

The later-Sprint current state itself is mutable and therefore must not substitute for append-only chronology.

## J5. Ambiguities / missing history

Confirmed:
- ACT6–13 phase transition chronology is not uniformly appended;
- formal audio trigger/playback history is not durable;
- some cinematic history is overwritten stage by stage.

This is IDA-005.

Separate operational failures IDA-001/004 can also prevent a complete history by halting progression, but they are not data-schema ambiguity findings.

## Method 9 disposition

**IDA-005 HIGH CONFIRMED**.

No additional data-forensics issue ID opened.
